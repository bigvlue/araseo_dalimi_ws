mission_manager:
  ros__parameters:
    robot_id: 0
    publish_hz: 1.0           # 미션 상태 발행 주기 (저빈도)
    nav2_action_timeout: 60.0 # 목적지당 최대 허용 시간 (초)
    arrival_tolerance_m: 0.05 # 도착 판정 거리 (50mm)
    use_nav2: true            # false면 차선 추종만 사용
    loop_back_on_complete: false
